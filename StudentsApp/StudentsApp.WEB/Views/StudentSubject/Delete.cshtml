@model StudentsApp.WEB.Models.Entities.StudentSubjectViewModel

@*@Html.Action("Details", "StudentSubject", new { id = Model.Id })*@
@Html.Partial("Details", Model)
<td>
    @using (Html.BeginForm("Delete", "StudentSubject", FormMethod.Post))
    {
        @Html.HiddenFor(m => m.Id)
        @Html.HiddenFor(m => m.TeacherId)
        @Html.HiddenFor(m => m.SubjectId)
        @Html.HiddenFor(m => m.StudentId)
        @Html.HiddenFor(m => m.FacultyId)
        @Html.Hidden("returnUrl", Request.Url.PathAndQuery)
        <button class="btn btn-default glyphicon glyphicon-remove"></button>
    }
</td>   